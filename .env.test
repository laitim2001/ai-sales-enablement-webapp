# 測試環境配置文件
# 用於工作流程整合測試

# 數據庫配置（測試數據庫）
# 注意：使用 Docker 容器端口 5433，密碼與開發環境相同
DATABASE_URL="postgresql://postgres:dev_password_123@localhost:5433/sales_enablement_test"

# JWT 配置
JWT_SECRET="test-jwt-secret-for-testing-purposes-only"
JWT_ACCESS_TOKEN_EXPIRES_IN="15m"
JWT_REFRESH_TOKEN_EXPIRES_IN="30d"

# Azure OpenAI 配置（測試環境可以使用 mock 值）
AZURE_OPENAI_API_KEY="test-api-key"
AZURE_OPENAI_ENDPOINT="https://test.openai.azure.com/"
AZURE_OPENAI_API_VERSION="2024-02-01"
AZURE_OPENAI_DEPLOYMENT_ID_GPT4="gpt-4"
AZURE_OPENAI_DEPLOYMENT_ID_EMBEDDINGS="text-embedding-ada-002"

# Redis 配置（測試環境可選）
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# 測試模式標記
NODE_ENV="test"
