/**
 * ================================================================
 * 檔案名稱: Auth Layout
 * 檔案用途: AI銷售賦能平台的認證頁面佈局組件
 * 開發階段: 生產就緒
 * ================================================================
 *
 * 功能索引:
 * 1. 認證頁面佈局 - 為登入和註冊頁面提供統一的佈局結構
 * 2. 最小化設計 - 簡潔的佈局，讓用戶專注於認證流程
 * 3. 全螢幕容器 - 確保認證頁面占滿整個視窗高度
 * 4. 靈活內容 - 支援任何認證相關的子頁面內容
 *
 * 組件特色:
 * - 簡潔佈局: 最小化的設計，避免分散用戶注意力
 * - 全螢幕高度: 使用min-h-screen確保頁面填滿視窗
 * - 內容靈活: 通過children prop支援登入、註冊等不同頁面
 * - SEO優化: 設定適當的頁面標題和描述
 * - 響應式設計: 自動適應不同裝置的螢幕尺寸
 *
 * 依賴組件:
 * - 無直接依賴組件，作為最基礎的佈局容器
 *
 * 注意事項:
 * - 此佈局應用於所有/(auth)/*路由
 * - 不包含導航元素，保持認證流程的純淨性
 * - 子組件需要自行處理背景和樣式
 *
 * 更新記錄:
 * - Week 1: 建立基礎認證佈局
 * - Week 2: 優化SEO元資料設定
 * ================================================================
 */

import { Metadata } from 'next'

// Next.js 元資料配置 - 針對認證相關頁面的SEO設定
export const metadata: Metadata = {
  title: {
    template: '%s | AI 銷售賦能平台',    // 子頁面標題模板(如：登入 | AI 銷售賦能平台)
    default: '認證 | AI 銷售賦能平台',   // 預設標題
  },
  description: '登入或註冊 AI 銷售賦能平台，開始您的智能銷售之旅',  // 頁面描述，用於搜索引擎優化
}

// AuthLayout組件的Props介面定義
interface AuthLayoutProps {
  children: React.ReactNode  // 認證頁面的子內容，如登入表單、註冊表單等
}

/**
 * 認證頁面佈局組件
 *
 * 這是所有認證相關頁面（登入、註冊等）的根佈局組件。
 * 提供最簡潔的容器結構，讓用戶專注於認證流程。
 *
 * @param children - 要渲染的認證頁面內容
 * @returns 認證頁面的基礎佈局結構
 */
export default function AuthLayout({ children }: AuthLayoutProps) {
  return (
    // 最小化佈局容器
    // min-h-screen: 確保容器至少占滿整個視窗高度
    // 不添加額外樣式，讓子組件完全控制視覺呈現
    <div className="min-h-screen">
      {children} {/* 渲染傳入的認證頁面內容 */}
    </div>
  )
}