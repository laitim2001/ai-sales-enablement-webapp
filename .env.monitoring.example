# ======================================
# AI Sales Enablement Platform
# Monitoring Configuration Example
# ======================================

# 監控後端選擇 (prometheus | azure | jaeger | console)
# 開發階段: prometheus
# 生產階段: azure
MONITORING_BACKEND=prometheus

# 服務名稱
SERVICE_NAME=ai-sales-platform

# ======================================
# Prometheus 配置（開發階段使用）
# ======================================
PROMETHEUS_PORT=9464
PROMETHEUS_ENDPOINT=/metrics

# ======================================
# Azure Application Insights 配置（生產階段使用）
# ======================================
# 從 Azure Portal 獲取連接字符串
# 格式: InstrumentationKey=xxx;IngestionEndpoint=https://xxx.in.applicationinsights.azure.com/;LiveEndpoint=https://xxx.livediagnostics.monitor.azure.com/
APPLICATIONINSIGHTS_CONNECTION_STRING=

# 採樣率 (0.0-1.0, 1.0 表示 100% 採樣)
# 生產環境建議 0.1-0.5 以控制成本
AZURE_SAMPLING_RATE=1.0

# 啟用 Live Metrics (實時監控)
AZURE_LIVE_METRICS=false

# ======================================
# Jaeger 配置（可選，用於分佈式追蹤）
# ======================================
JAEGER_ENDPOINT=http://localhost:14268/api/traces
JAEGER_AGENT_HOST=localhost
JAEGER_AGENT_PORT=6831

# ======================================
# Console 輸出配置（開發用）
# ======================================
CONSOLE_TELEMETRY=false

# ======================================
# 遷移指南
# ======================================
# 開發階段 → 生產階段遷移只需 3 步驟:
#
# 1. 修改後端選擇:
#    MONITORING_BACKEND=azure
#
# 2. 設置 Azure 連接字符串:
#    APPLICATIONINSIGHTS_CONNECTION_STRING="InstrumentationKey=xxx;..."
#
# 3. 設置適當的採樣率 (可選):
#    AZURE_SAMPLING_RATE=0.2  # 20% 採樣以控制成本
#
# 完成！無需修改任何代碼。
